# Python requirements for Trading Bot
# Server (Render): install all
# Termux (dev): skip heavy packages (numpy, pandas)

# Core
requests>=2.31.0
python-dotenv>=1.0.0

# Flask / API (for remote control)
flask>=3.0.0
PyJWT>=2.8.0

# Data / ML (install on server only)
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Solana (optional, for DEX trading)
# solathon>=0.3.0  # Uncomment for Solana support

# Optional: TA-Lib for advanced indicators (needs system lib)
# ta-lib>=0.4.0
