
# Core dependencies
# Data analysis (for backtesting)
# Database
# Exchange integration (unified API for all exchanges)
# Install with: pip install -r requirements.txt
# Solana blockchain (optional - use solana_simple.py if solders fails)
# Testing
# Trading Bot Dependencies
# Type hints

# Encryption for API keys
cryptography>=42.0.0

# System monitoring (optional but recommended)
psutil>=5.9.0

# Web dashboard
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
python-engineio>=4.8.0
python-socketio>=5.10.0

# WebSocket support
websockets>=12.0

# Data broker layer - Institutional grade data
# CoinAPI: 400+ exchanges, tick-level historical
# Amberdata: On-chain + derivatives + DeFi data
# LunarCrush: Crypto social sentiment
requests>=2.31.0

# Data processing
numpy>=1.24.0
pandas>=2.1.0

# Database
aiosqlite>=0.19.0

# Solana (optional)
# solana>=0.30.0   # Commented out - depends on solders
# solders>=0.21.0  # Commented out - has build issues on Termux
base58>=2.1.1
solathon

# Exchange connectors
ccxt>=4.2.0

# Environment variables
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
